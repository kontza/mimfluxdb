version: "0.1"
database:
    db: postgres
    dsn: "host=localhost user=postgres password=some-very-secret-password-here dbname=atmospi port=5432 sslmode=disable TimeZone=Europe/Helsinki"
    tables:
        - temperature
        - count
        - rssi
        - device
